# EMQX Minimal Configuration - Dashboard Only

## Node Configuration
node.name = emqx@127.0.0.1
node.cookie = emqx_secret_change_this

## DISABLE ALL MQTT LISTENERS
listeners.tcp.default.enabled = false
listeners.ssl.default.enabled = false
listeners.ws.default.enabled = false
listeners.wss.default.enabled = false

## ENABLE ONLY DASHBOARD (HTTP)
dashboard.listeners.http.bind = "0.0.0.0:18083"
dashboard.listeners.http.enable = true
dashboard.listeners.http.max_connections = 512

## DISABLE DASHBOARD HTTPS
dashboard.listeners.https.enable = false

## Authentication - Built-in Database
authentication = [
  {
    enable = true
    backend = built_in_database
    type = password_based
    user_id_type = username
    password_hash_type = sha256
  }
]

## Disable unused features
persistence.enable = false
cluster.discovery = manual

## Logging
log {
  level = warning
  console_handler {
    level = warning
  }
}
